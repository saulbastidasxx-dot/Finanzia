import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
Deno.serve(async(req)=>{if(req.method!=='POST')return new Response('Method not allowed',{status:405});const auth=req.headers.get('Authorization');if(!auth)return new Response('Unauthorized',{status:401});const url=Deno.env.get('SUPABASE_URL')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const uc=createClient(url,anon,{global:{headers:{Authorization:auth}}});const {data:{user},error}=await uc.auth.getUser();if(error||!user)return new Response('Unauthorized',{status:401});const admin=createClient(url,service);for(const table of ['sync_changes','sync_state','recurring_payments','transactions','budgets','goals','debts','accounts']){const {error:e}=await admin.from(table).delete().eq('user_id',user.id);if(e)return new Response(`Delete ${table}: ${e.message}`,{status:500})}const {error:pe}=await admin.from('profiles').delete().eq('id',user.id);if(pe)return new Response(pe.message,{status:500});const result=await admin.auth.admin.deleteUser(user.id);if(result.error)return new Response(result.error.message,{status:500});return Response.json({ok:true})})