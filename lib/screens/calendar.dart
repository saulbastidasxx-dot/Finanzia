import 'package:flutter/material.dart';import 'package:intl/intl.dart';import '../models/finance_models.dart';import '../models/finance_store.dart';final money=NumberFormat.currency(symbol:'\$');
class CalendarScreen extends StatelessWidget{final FinanceStore store;const CalendarScreen({super.key,required this.store});@override Widget build(BuildContext c){final items=[...store.recurring]..sort((a,b)=>a.dayOfMonth.compareTo(b.dayOfMonth));final out=items.where((x)=>x.isExpense&&x.active).fold<double>(0,(s,x)=>s+x.amount),inc=items.where((x)=>!x.isExpense&&x.active).fold<double>(0,(s,x)=>s+x.amount);return SafeArea(child:ListView(padding:const EdgeInsets.all(20),children:[Row(children:[Expanded(child:Text('Calendario',style:Theme.of(c).textTheme.headlineMedium?.copyWith(fontWeight:FontWeight.w800))),IconButton(onPressed:()=>_edit(c),icon:const Icon(Icons.add_circle_outline))]),const SizedBox(height:8),const Text('Pagos e ingresos recurrentes del mes'),const SizedBox(height:12),Wrap(spacing:12,children:[Chip(label:Text('Pagos ${money.format(out)}')),Chip(label:Text('Ingresos ${money.format(inc)}'))]),const SizedBox(height:16),if(items.isEmpty)const Card(child:Padding(padding:EdgeInsets.all(24),child:Text('Aún no tienes movimientos recurrentes.'))),...items.map((r)=>Card(child:ListTile(onTap:()=>_edit(c,existing:r),leading:CircleAvatar(child:Text('${r.dayOfMonth}')),title:Text(r.name),subtitle:Text('${r.category} · ${r.isExpense?'Gasto':'Ingreso'}${r.active?'':' · Pausado'}'),trailing:Text(money.format(r.amount),style:const TextStyle(fontWeight:FontWeight.w700)))))]));}
void _edit(BuildContext c,{RecurringPayment? existing}){if(store.accounts.isEmpty)return;final name=TextEditingController(text:existing?.name??''),amount=TextEditingController(text:existing?.amount.toStringAsFixed(2)??''),day=TextEditingController(text:existing?.dayOfMonth.toString()??'');String account=existing?.accountId??store.accounts.first.id,category=existing?.category??store.categories.first;bool expense=existing?.isExpense??true,active=existing?.active??true;showDialog(context:c,builder:(d)=>StatefulBuilder(builder:(d,set)=>AlertDialog(title:Text(existing==null?'Nuevo recurrente':'Editar recurrente'),content:SingleChildScrollView(child:Column(mainAxisSize:MainAxisSize.min,children:[TextField(controller:name,decoration:const InputDecoration(labelText:'Nombre')),TextField(controller:amount,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Monto')),TextField(controller:day,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Día del mes (1–28)')),DropdownButtonFormField(value:category,items:store.categories.map((x)=>DropdownMenuItem(value:x,child:Text(x))).toList(),onChanged:(v)=>category=v!,decoration:const InputDecoration(labelText:'Categoría')),DropdownButtonFormField(value:account,items:store.accounts.map((a)=>DropdownMenuItem(value:a.id,child:Text(a.name))).toList(),onChanged:(v)=>account=v!,decoration:const InputDecoration(labelText:'Cuenta')),SwitchListTile(value:expense,onChanged:(v)=>set(()=>expense=v),title:Text(expense?'Gasto':'Ingreso')),SwitchListTile(value:active,onChanged:(v)=>set(()=>active=v),title:const Text('Activo'))])),actions:[TextButton(onPressed:()=>Navigator.pop(d),child:const Text('Cancelar')),FilledButton(onPressed:()async{final a=double.tryParse(amount.text.replaceAll(',','.'))??0,dy=int.tryParse(day.text)??0;if(name.text.trim().isEmpty||a<=0||dy<1||dy>28)return;final x=RecurringPayment(id:existing?.id??DateTime.now().microsecondsSinceEpoch.toString(),name:name.text.trim(),category:category,accountId:account,amount:a,dayOfMonth:dy,isExpense:expense,active:active);if(existing==null){await store.addRecurring(x);}else{await store.updateRecurring(x);}if(d.mounted)Navigator.pop(d);},child:const Text('Guardar'))])));}}